const localeTexts = {
  configFields: {
    advancedPublishingConfig: {
      label: "Configuration JSON",
      help: "Put here the JSON configuration for the component",
      placeholder: "json configuration",
      instruction: "Please, put here the JSON configuration for the component",
    },
  },
};

export default localeTexts;
